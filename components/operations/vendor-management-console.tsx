"use client";

import { useState } from "react";
import Link from "next/link";
import { AnimatePresence, motion } from "motion/react";
import { AlertTriangle, BadgeCheck, Banknote, Boxes, Building2, ChevronRight, CircleAlert, ClipboardCheck, FileCheck2, Gauge, PackageCheck, Plus, RefreshCw, Send, ShieldCheck, Truck, UserPlus, Users, X } from "lucide-react";
import { AdminActionButton } from "@/components/operations/admin-action-button";
import { ScrollReveal } from "@/components/motion/scroll-motion";
import { VendorRealtimeSync } from "@/components/vendor/vendor-realtime-sync";
import { addPurchaseOrderLine, addVendorCatalogItem, addVendorImprovementPlan, addVendorReview, calculateVendorPerformance, createVendor, generateReorderRequisitions, inviteVendorUser, issuePurchaseOrder, matchVendorInvoice, recordGoodsReceipt, recordVendorPayment, reviewVendor, verifyVendorDocument } from "@/app/(app)/dashboard/vendors/actions";
import { hasPermission, type OrganizationRole } from "@/lib/auth/permissions";
import { formatPhpCurrency } from "@/lib/utils";
import type { Database } from "@/types/database";

type Tables = Database["public"]["Tables"];
type Row<K extends keyof Tables> = Tables[K]["Row"];
type Vendor = Row<"suppliers">;
type Tab = "overview" | "catalog" | "orders" | "logistics" | "billing" | "performance";

type Props = {
  preview?: boolean;
  organizationId?: string;
  organizationSlug?: string;
  role?: OrganizationRole;
  selectedVendorId?: string;
  success?: string;
  error?: string;
  setupError?: string;
  vendors?: Vendor[];
  vendorUsers?: Row<"vendor_users">[];
  contacts?: Row<"vendor_contacts">[];
  addresses?: Row<"vendor_addresses">[];
  shippingRules?: Row<"vendor_shipping_rules">[];
  documents?: Row<"vendor_documents">[];
  catalog?: Row<"vendor_catalog_items">[];
  orders?: Row<"procurement_records">[];
  orderLines?: Row<"procurement_record_lines">[];
  shipments?: Row<"shipments">[];
  shipmentLines?: Row<"shipment_lines">[];
  invoices?: Row<"vendor_invoices">[];
  payments?: Row<"payment_records">[];
  performance?: Row<"vendor_performance_snapshots">[];
  reviews?: Row<"vendor_reviews">[];
  improvements?: Row<"vendor_improvement_plans">[];
  fileUrls?: Record<string, string>;
  products?: Array<{ id: string; sku: string; name: string }>;
  warehouses?: Array<{ id: string; code: string; name: string }>;
};

const tabs: Array<{ key: Tab; label: string; icon: typeof Building2 }> = [
  { key: "overview", label: "Overview", icon: Building2 },
  { key: "catalog", label: "Catalog", icon: Boxes },
  { key: "orders", label: "Orders", icon: ClipboardCheck },
  { key: "logistics", label: "Logistics", icon: Truck },
  { key: "billing", label: "Billing", icon: Banknote },
  { key: "performance", label: "Performance", icon: Gauge },
];

export function VendorManagementConsole(props: Props) {
  const [drawerOpen, setDrawerOpen] = useState(false);
  const [tab, setTab] = useState<Tab>("overview");
  const vendors = props.vendors ?? [];
  const selected = vendors.find((vendor) => vendor.id === props.selectedVendorId) ?? vendors[0];
  const canManage = props.role ? hasPermission(props.role, "vendors.manage") : true;
  const approved = vendors.filter((vendor) => vendor.onboarding_status === "approved").length;
  const pending = vendors.filter((vendor) => ["submitted", "under_review", "changes_requested"].includes(vendor.onboarding_status)).length;
  const atRisk = vendors.filter((vendor) => ["medium", "high"].includes(vendor.risk_rating)).length;
  const overdueInvoices = (props.invoices ?? []).filter((invoice) => invoice.status !== "paid" && new Date(invoice.due_date) < new Date()).length;

  if (props.preview) return <main className="p-6 lg:p-10"><Empty title="Vendor management preview" body="Connect the workspace backend and apply migration 013 to activate vendor onboarding and portal workflows." /></main>;

  return <main className="min-w-0 p-4 sm:p-6 lg:p-10">
    <ScrollReveal><header className="flex flex-col gap-5 xl:flex-row xl:items-end xl:justify-between"><div><div className="flex flex-wrap items-center gap-2"><p className="font-mono text-xs uppercase tracking-[.12em] text-blue-700">Procurement · Partner network</p>{props.organizationId && <VendorRealtimeSync organizationId={props.organizationId} badge />}</div><h1 className="mt-3 text-3xl font-bold tracking-[-.05em] sm:text-4xl">Vendor management</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted">Qualify partners, control orders, receive goods, reconcile invoices, and improve performance from one auditable workspace.</p></div><div className="flex flex-wrap gap-2">{props.organizationSlug && <Link href={`/vendors/apply/${props.organizationSlug}`} target="_blank" className="inline-flex h-11 items-center gap-2 rounded-xl border border-line bg-white px-4 text-sm font-bold"><Building2 size={14} /> Public application</Link>}{canManage && <form action={generateReorderRequisitions}><input type="hidden" name="organizationId" value={props.organizationId} /><AdminActionButton variant="secondary"><RefreshCw size={14} /> Generate reorder requests</AdminActionButton></form>}{canManage && <button onClick={() => setDrawerOpen(true)} className="inline-flex h-11 items-center gap-2 rounded-xl bg-accent px-4 text-sm font-bold text-white shadow-[0_14px_34px_rgba(37,99,235,.2)]"><Plus size={15} /> Add vendor</button>}</div></header></ScrollReveal>

    {(props.success || props.error || props.setupError) && <div className={`mt-6 flex items-start gap-3 rounded-xl border px-4 py-3 text-sm ${props.error || props.setupError ? "border-red-200 bg-red-50 text-red-800" : "border-blue-200 bg-blue-50 text-blue-800"}`}>{props.error || props.setupError ? <CircleAlert size={16} /> : <BadgeCheck size={16} />}<span>{props.error ?? props.setupError ?? props.success}</span></div>}

    <ScrollReveal className="mt-6"><div className="grid overflow-hidden rounded-2xl border border-line bg-white shadow-sm sm:grid-cols-2 xl:grid-cols-4"><Metric label="All vendors" value={vendors.length} detail="Partner master" /><Metric label="Approved" value={approved} detail="Transaction ready" /><Metric label="Pending review" value={pending} detail="Needs action" /><Metric label="Risk / overdue" value={atRisk + overdueInvoices} detail={`${atRisk} vendor · ${overdueInvoices} invoice`} /></div></ScrollReveal>

    <div className="mt-6 grid min-w-0 gap-5 xl:grid-cols-[300px_minmax(0,1fr)]">
      <ScrollReveal><section className="overflow-hidden rounded-2xl border border-line bg-white shadow-sm"><div className="border-b border-line px-5 py-4"><h2 className="font-bold">Vendor directory</h2><p className="mt-1 text-xs text-muted">{vendors.length} standardized profiles</p></div><div className="max-h-[680px] overflow-y-auto p-2">{vendors.map((vendor) => <Link key={vendor.id} href={`/dashboard/vendors?vendor=${vendor.id}`} className={`mb-1 flex items-center gap-3 rounded-xl p-3 transition ${selected?.id === vendor.id ? "bg-blue-50 text-blue-800" : "hover:bg-mist"}`}><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-white font-mono text-xs font-bold shadow-sm">{vendor.code.slice(0, 2)}</span><span className="min-w-0 flex-1"><strong className="block truncate text-sm">{vendor.name}</strong><span className="mt-1 block truncate text-xs text-muted">{humanize(vendor.vendor_category)}</span></span><ChevronRight size={14} /></Link>)}{!vendors.length && <Empty title="No vendors yet" body="Create the first vendor application to start onboarding." compact />}</div></section></ScrollReveal>

      {selected ? <ScrollReveal><section className="min-w-0 overflow-hidden rounded-2xl border border-line bg-white shadow-sm"><div className="border-b border-line p-5 sm:p-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-start"><span className="grid size-12 shrink-0 place-items-center rounded-2xl bg-gradient-to-br from-blue-600 to-blue-800 text-sm font-bold text-white">{selected.code.slice(0, 2)}</span><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h2 className="text-xl font-bold">{selected.name}</h2><Status value={selected.onboarding_status} /></div><p className="mt-1 text-sm text-muted">{selected.legal_name ?? selected.name} · {selected.code}</p></div><div className="sm:ml-auto"><span className="rounded-full bg-slate-100 px-3 py-1.5 text-xs font-semibold">Risk: {humanize(selected.risk_rating)}</span></div></div><div className="mt-5 flex gap-1 overflow-x-auto rounded-xl bg-mist p-1">{tabs.map(({ key, label, icon: Icon }) => <button key={key} onClick={() => setTab(key)} className={`flex shrink-0 items-center gap-2 rounded-lg px-3 py-2 text-xs font-bold transition ${tab === key ? "bg-white text-blue-700 shadow-sm" : "text-muted"}`}><Icon size={14} />{label}</button>)}</div></div>
        <AnimatePresence mode="wait"><motion.div key={tab} initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0 }} className="p-5 sm:p-6">{tab === "overview" && <Overview {...props} vendor={selected} canManage={canManage} />}{tab === "catalog" && <Catalog {...props} vendor={selected} canManage={canManage} />}{tab === "orders" && <Orders {...props} vendor={selected} canManage={canManage} />}{tab === "logistics" && <Logistics {...props} vendor={selected} canManage={canManage} />}{tab === "billing" && <Billing {...props} vendor={selected} canManage={canManage} />}{tab === "performance" && <Performance {...props} vendor={selected} canManage={canManage} />}</motion.div></AnimatePresence>
      </section></ScrollReveal> : <Empty title="Select a vendor" body="Vendor details and workflows will appear here." />}
    </div>

    <AnimatePresence>{drawerOpen && <Drawer title="Create vendor application" onClose={() => setDrawerOpen(false)}><form action={createVendor} className="grid gap-4 p-5 sm:grid-cols-2 sm:p-6"><input type="hidden" name="organizationId" value={props.organizationId} /><Field name="code" label="Vendor code" required placeholder="VND-001" /><Field name="name" label="Trading name" required /><Field name="legalName" label="Legal name" required /><Field name="email" label="Contact email" type="email" required /><Field name="phone" label="Phone" /><Select name="category" label="Category" values={["raw_material", "finished_goods", "spare_parts", "logistics_provider", "services"]} /><Field name="taxId" label="Tax ID" /><Field name="registrationNumber" label="Registration number" /><Field name="paymentTermsDays" label="Payment terms (days)" type="number" defaultValue="30" min="0" /><Field name="creditLimit" label="Credit limit (₱)" type="number" defaultValue="0" min="0" /><Field name="leadTimeDays" label="Lead time (days)" type="number" defaultValue="0" min="0" /><Field name="deliveryCapacity" label="Delivery capacity" type="number" defaultValue="0" min="0" /><Field name="serviceAreas" label="Service areas · comma separated" className="sm:col-span-2" /><Field name="deliveryMethods" label="Delivery methods · comma separated" className="sm:col-span-2" /><Field name="address" label="Business address" className="sm:col-span-2" /><Field name="city" label="City" /><div className="sm:col-span-2"><AdminActionButton className="w-full">Create vendor application</AdminActionButton></div></form></Drawer>}</AnimatePresence>
  </main>;
}

function Overview(props: Props & { vendor: Vendor; canManage: boolean }) {
  const { vendor } = props;
  const users = (props.vendorUsers ?? []).filter((row) => row.supplier_id === vendor.id);
  const contacts = (props.contacts ?? []).filter((row) => row.supplier_id === vendor.id);
  const addresses = (props.addresses ?? []).filter((row) => row.supplier_id === vendor.id);
  const shippingRules = (props.shippingRules ?? []).filter((row) => row.supplier_id === vendor.id);
  const docs = (props.documents ?? []).filter((row) => row.supplier_id === vendor.id);
  return <div className="grid gap-5 lg:grid-cols-2"><Panel title="Company profile"><Info label="Category" value={humanize(vendor.vendor_category)} /><Info label="Contact" value={vendor.contact_email ?? "Not provided"} /><Info label="Terms" value={`Net ${vendor.payment_terms_days} · ${formatPhpCurrency(vendor.credit_limit)} credit`} /><Info label="Delivery" value={`${vendor.lead_time_days ?? 0} days · ${vendor.service_areas.join(", ") || "No service area"}`} /><Info label="Address" value={[vendor.address_line, vendor.city, vendor.province].filter(Boolean).join(", ") || "Not provided"} /><div className="mt-4 grid grid-cols-3 gap-2"><Mini label="Contacts" value={contacts.length} /><Mini label="Addresses" value={addresses.length} /><Mini label="Shipping rules" value={shippingRules.length} /></div></Panel><Panel title="Compliance"><div className="grid grid-cols-2 gap-3"><Mini label="Documents" value={docs.length} /><Mini label="Verified" value={docs.filter((doc) => doc.status === "verified").length} /><Mini label="Portal users" value={users.length} /><Mini label="Active users" value={users.filter((user) => user.status === "active").length} /></div>{docs.slice(0, 6).map((doc) => <div key={doc.id} className="mt-3 rounded-xl bg-mist p-3"><div className="flex items-center gap-3"><FileCheck2 size={16} className="text-blue-700" /><span className="min-w-0 flex-1 truncate text-sm">{doc.title}</span><Status value={doc.status} /></div>{props.fileUrls?.[doc.storage_path] && <a href={props.fileUrls[doc.storage_path]} target="_blank" rel="noreferrer" className="mt-2 inline-block text-xs font-bold text-blue-700 hover:underline">Open secure document</a>}{props.canManage && doc.status === "submitted" && <form action={verifyVendorDocument} className="mt-3 flex flex-wrap gap-2"><Hidden org={props.organizationId} vendor={vendor.id} /><input type="hidden" name="documentId" value={doc.id} /><button name="status" value="verified" className="rounded-lg bg-emerald-600 px-3 py-2 text-xs font-bold text-white">Verify</button><button name="status" value="rejected" className="rounded-lg border border-red-200 bg-white px-3 py-2 text-xs font-bold text-red-700">Reject</button></form>}</div>)}</Panel>{props.canManage && <Panel title="Review and approve"><form action={reviewVendor} className="space-y-3"><Hidden org={props.organizationId} vendor={vendor.id} /><Select name="onboardingStatus" label="Decision" values={["under_review", "changes_requested", "approved", "rejected", "suspended", "archived"]} defaultValue={vendor.onboarding_status} /><Select name="riskRating" label="Risk rating" values={["unrated", "low", "medium", "high"]} defaultValue={vendor.risk_rating} /><TextArea name="notes" label="Review notes" defaultValue={vendor.notes ?? ""} /><AdminActionButton className="w-full"><ShieldCheck size={14} /> Save review</AdminActionButton></form></Panel>}{props.canManage && <Panel title="Invite portal user"><form action={inviteVendorUser} className="space-y-3"><Hidden org={props.organizationId} vendor={vendor.id} /><Field name="fullName" label="Full name" required /><Field name="email" label="Email" type="email" required /><Select name="role" label="Portal role" values={["admin", "member", "finance", "logistics"]} /><AdminActionButton className="w-full"><UserPlus size={14} /> Send secure invitation</AdminActionButton></form></Panel>}</div>;
}

function Catalog(props: Props & { vendor: Vendor; canManage: boolean }) {
  const rows = (props.catalog ?? []).filter((row) => row.supplier_id === props.vendor.id);
  return <div className="grid gap-5 lg:grid-cols-[1fr_340px]"><Panel title="Vendor catalog">{rows.map((row) => <article key={row.id} className="border-b border-line py-4 first:pt-0 last:border-0"><div className="flex gap-3"><div className="min-w-0 flex-1"><h3 className="font-semibold">{row.description}</h3><p className="mt-1 font-mono text-xs text-blue-700">{row.vendor_item_code}</p></div><strong>{formatPhpCurrency(row.unit_price)}</strong></div><p className="mt-2 text-xs text-muted">MOQ {row.minimum_order_quantity} · Lead {row.lead_time_days} days · {row.packaging_specs || "Standard packaging"}</p></article>)}{!rows.length && <Empty title="No catalog items" body="Map the vendor's first product or service." compact />}</Panel>{props.canManage && <Panel title="Add catalog item"><form action={addVendorCatalogItem} className="space-y-3"><Hidden org={props.organizationId} vendor={props.vendor.id} /><SelectOptions name="productId" label="Internal product mapping" options={(props.products ?? []).map((p) => ({ value: p.id, label: `${p.sku} · ${p.name}` }))} optional /><Field name="itemCode" label="Vendor item code" required /><Field name="description" label="Description" required /><Field name="unitPrice" label="Unit price (₱)" type="number" min="0" step="0.01" required /><Field name="moq" label="Minimum order quantity" type="number" min="0.0001" step="0.0001" defaultValue="1" required /><Field name="leadTimeDays" label="Lead time (days)" type="number" min="0" defaultValue="0" /><Field name="deliveryWindowDays" label="Delivery window (days)" type="number" min="0" defaultValue="0" /><Field name="packagingSpecs" label="Packaging specifications" /><AdminActionButton className="w-full">Add catalog item</AdminActionButton></form></Panel>}</div>;
}

function Orders(props: Props & { vendor: Vendor; canManage: boolean }) {
  const orders = (props.orders ?? []).filter((row) => row.supplier_id === props.vendor.id);
  return <div className="space-y-4">{orders.map((order) => { const lines = (props.orderLines ?? []).filter((line) => line.procurement_record_id === order.id); return <Panel key={order.id} title={`${order.reference} · ${order.title}`} action={<Status value={order.status} />}><div className="grid gap-3 sm:grid-cols-3"><Mini label="Value" value={formatPhpCurrency(Number(order.amount ?? 0))} /><Mini label="Delivery" value={order.delivery_date ?? order.due_at?.slice(0, 10) ?? "Not set"} /><Mini label="Version" value={`v${order.version}`} /></div><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[540px] text-left text-sm"><thead className="text-xs uppercase text-muted"><tr><th className="py-2">Item</th><th>Qty</th><th>Received</th><th>Unit price</th></tr></thead><tbody>{lines.map((line) => <tr key={line.id} className="border-t border-line"><td className="py-3">{line.description}</td><td>{line.quantity}</td><td>{line.received_quantity}</td><td>{formatPhpCurrency(line.unit_price)}</td></tr>)}</tbody></table></div>{props.canManage && <details className="mt-4 rounded-xl bg-mist p-3"><summary className="cursor-pointer text-sm font-bold">Add PO line</summary><form action={addPurchaseOrderLine} className="mt-4 grid gap-3 sm:grid-cols-2"><Hidden org={props.organizationId} vendor={props.vendor.id} /><input type="hidden" name="purchaseOrderId" value={order.id} /><SelectOptions name="productId" label="Product" options={(props.products ?? []).map((p) => ({ value: p.id, label: `${p.sku} · ${p.name}` }))} optional /><Field name="description" label="Description" required /><Field name="quantity" label="Quantity" type="number" min="0.0001" step="0.0001" required /><Field name="unitPrice" label="Unit price (₱)" type="number" min="0" step="0.01" required /><Field name="taxRate" label="Tax rate (%)" type="number" min="0" max="100" defaultValue="0" /><Field name="promisedDate" label="Promised date" type="date" /><AdminActionButton>Add line</AdminActionButton></form></details>}{props.canManage && ["draft", "approved", "revised"].includes(order.status) && <form action={issuePurchaseOrder} className="mt-3"><Hidden org={props.organizationId} vendor={props.vendor.id} /><input type="hidden" name="purchaseOrderId" value={order.id} /><AdminActionButton><Send size={14} /> Issue to vendor</AdminActionButton></form>}</Panel>; })}{!orders.length && <Empty title="No purchase orders" body="Create a purchase order from Procurement and assign this vendor." />}</div>;
}

function Logistics(props: Props & { vendor: Vendor; canManage: boolean }) {
  const shipments = (props.shipments ?? []).filter((row) => row.supplier_id === props.vendor.id);
  return <div className="space-y-4">{shipments.map((shipment) => <Panel key={shipment.id} title={`${shipment.reference} · ${shipment.title}`} action={<Status value={shipment.status} />}><div className="grid gap-3 sm:grid-cols-3"><Mini label="Carrier" value={shipment.carrier ?? "Not assigned"} /><Mini label="Tracking" value={shipment.tracking_number ?? "Not provided"} /><Mini label="ETA" value={shipment.expected_arrival_at?.slice(0, 16).replace("T", " ") ?? "Not set"} /></div>{(props.shipmentLines ?? []).filter((line) => line.shipment_id === shipment.id).map((line) => <article key={line.id} className="mt-4 rounded-xl border border-line p-4"><div className="flex justify-between gap-3"><span className="font-semibold">{line.description}</span><span className="font-mono text-sm">{line.received_quantity}/{line.quantity}</span></div>{props.canManage && line.received_quantity < line.quantity && <form action={recordGoodsReceipt} className="mt-4 grid gap-3 sm:grid-cols-2"><Hidden org={props.organizationId} vendor={props.vendor.id} /><input type="hidden" name="shipmentLineId" value={line.id} /><Field name="reference" label="Receipt reference" placeholder="GRN-0001" required /><Field name="acceptedQuantity" label="Accepted quantity" type="number" min="0" step="0.0001" defaultValue="0" /><Field name="rejectedQuantity" label="Rejected quantity" type="number" min="0" step="0.0001" defaultValue="0" /><Field name="rejectionReason" label="Rejection / shortage reason" /><Check name="qualityOk" label="Quality accepted" /><Check name="conditionOk" label="Condition accepted" /><Check name="packagingOk" label="Packaging accepted" /><AdminActionButton><PackageCheck size={14} /> Post receipt</AdminActionButton></form>}</article>)}</Panel>)}{!shipments.length && <Empty title="No shipments" body="Shipments created by the vendor will appear here for receiving." />}</div>;
}

function Billing(props: Props & { vendor: Vendor; canManage: boolean }) {
  const invoices = (props.invoices ?? []).filter((row) => row.supplier_id === props.vendor.id);
  return <div className="space-y-4">{invoices.map((invoice) => { const invoicePayments = (props.payments ?? []).filter((payment) => payment.invoice_id === invoice.id); return <Panel key={invoice.id} title={`Invoice ${invoice.invoice_number}`} action={<Status value={invoice.status} />}><div className="grid gap-3 sm:grid-cols-4"><Mini label="Total" value={formatPhpCurrency(invoice.total_amount)} /><Mini label="Invoice date" value={invoice.invoice_date} /><Mini label="Due date" value={invoice.due_date} /><Mini label="Paid" value={formatPhpCurrency(invoicePayments.filter((p) => p.status === "paid").reduce((sum, p) => sum + Number(p.amount), 0))} /></div>{invoice.mismatch_reason && <p className="mt-4 flex gap-2 rounded-xl bg-amber-50 p-3 text-sm text-amber-800"><AlertTriangle size={16} />{invoice.mismatch_reason}</p>}{props.canManage && ["submitted", "matching", "exception"].includes(invoice.status) && <form action={matchVendorInvoice} className="mt-4"><Hidden org={props.organizationId} vendor={props.vendor.id} /><input type="hidden" name="invoiceId" value={invoice.id} /><AdminActionButton variant="secondary"><ClipboardCheck size={14} /> Run three-way match</AdminActionButton></form>}{props.canManage && invoice.status === "approved" && <details className="mt-4 rounded-xl bg-mist p-3"><summary className="cursor-pointer text-sm font-bold">Record payment</summary><form action={recordVendorPayment} className="mt-4 grid gap-3 sm:grid-cols-2"><Hidden org={props.organizationId} vendor={props.vendor.id} /><input type="hidden" name="invoiceId" value={invoice.id} /><Field name="reference" label="Payment reference" required /><Field name="amount" label="Amount (₱)" type="number" min="0.01" step="0.01" defaultValue={invoice.total_amount} required /><Field name="paymentMethod" label="Payment method" /><Select name="status" label="Status" values={["pending", "approved", "scheduled", "paid"]} /><AdminActionButton>Save payment</AdminActionButton></form></details>}</Panel>; })}{!invoices.length && <Empty title="No invoices" body="Vendor-submitted invoices will appear here for matching and payment." />}</div>;
}

function Performance(props: Props & { vendor: Vendor; canManage: boolean }) {
  const score = (props.performance ?? []).find((row) => row.supplier_id === props.vendor.id);
  const reviews = (props.reviews ?? []).filter((row) => row.supplier_id === props.vendor.id);
  const improvements = (props.improvements ?? []).filter((row) => row.supplier_id === props.vendor.id);
  return <div className="grid gap-5 lg:grid-cols-[1fr_340px]"><Panel title="Latest 90-day scorecard">{score ? <><div className="flex items-center gap-5"><div className="grid size-28 place-items-center rounded-full border-[10px] border-blue-100 text-3xl font-bold text-blue-700">{score.overall_score.toFixed(0)}</div><div><h3 className="font-bold">Overall vendor score</h3><p className="mt-1 text-sm text-muted">{score.period_start} to {score.period_end}</p><p className="mt-2 text-xs text-muted">Based on {score.order_count} purchase orders · {score.response_time_hours.toFixed(1)}h response</p></div></div><div className="mt-6 grid gap-3 sm:grid-cols-2"><Score label="On-time delivery" value={score.on_time_delivery_rate} /><Score label="Fulfillment" value={score.fulfillment_rate} /><Score label="Quality" value={score.quality_score} /><Score label="Accuracy" value={score.accuracy_score} /></div></> : <Empty title="No scorecard" body="Calculate the first performance snapshot after transactions are recorded." compact />}{props.canManage && <form action={calculateVendorPerformance} className="mt-5"><Hidden org={props.organizationId} vendor={props.vendor.id} /><AdminActionButton variant="secondary"><RefreshCw size={14} /> Recalculate score</AdminActionButton></form>}</Panel><div className="space-y-5"><Panel title="Lifecycle review">{reviews.slice(0, 3).map((review) => <div key={review.id} className="border-b border-line py-3 text-sm last:border-0"><div className="flex justify-between gap-3"><strong>{humanize(review.action)}</strong><span className="text-xs text-muted">{review.review_date}</span></div><p className="mt-1 text-muted">{review.summary}</p></div>)}{props.canManage && <form action={addVendorReview} className="mt-4 space-y-3"><Hidden org={props.organizationId} vendor={props.vendor.id} /><Select name="action" label="Action" values={["preferred", "renew", "coaching", "warning", "improvement_plan", "suspend", "deactivate"]} /><TextArea name="summary" label="Review summary" required /><Field name="nextReviewDate" label="Next review" type="date" /><AdminActionButton className="w-full">Record review</AdminActionButton></form>}</Panel><Panel title="Improvement plans">{improvements.slice(0, 3).map((plan) => <div key={plan.id} className="mb-3 rounded-xl bg-mist p-3"><div className="flex justify-between gap-2"><strong className="text-sm">{plan.title}</strong><Status value={plan.status} /></div><p className="mt-2 text-xs text-muted">Due {plan.due_date}</p><p className="mt-2 text-sm">{plan.objectives}</p></div>)}{props.canManage && <form action={addVendorImprovementPlan} className="mt-4 space-y-3"><Hidden org={props.organizationId} vendor={props.vendor.id} /><Field name="title" label="Plan title" required /><TextArea name="objectives" label="Objectives and required actions" required /><Field name="dueDate" label="Due date" type="date" required /><AdminActionButton className="w-full">Create improvement plan</AdminActionButton></form>}</Panel></div></div>;
}

function Drawer({ title, onClose, children }: { title: string; onClose: () => void; children: React.ReactNode }) { return <><motion.button aria-label="Close form" onClick={onClose} className="fixed inset-0 z-[70] bg-ink/40 backdrop-blur-[2px]" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} /><motion.aside className="fixed inset-y-0 right-0 z-[80] flex h-dvh w-full flex-col overflow-y-auto bg-[#f8faff] shadow-[-24px_0_70px_rgba(7,23,45,.18)] sm:max-w-[680px]" initial={{ x: "100%" }} animate={{ x: 0 }} exit={{ x: "100%" }} transition={{ type: "spring", stiffness: 320, damping: 32 }}><div className="sticky top-0 z-10 flex items-center border-b border-line bg-white px-5 py-4"><h2 className="text-xl font-bold">{title}</h2><button type="button" onClick={onClose} className="ml-auto grid size-10 place-items-center rounded-xl border border-line"><X size={17} /></button></div>{children}</motion.aside></>; }
function Panel({ title, action, children }: { title: string; action?: React.ReactNode; children: React.ReactNode }) { return <section className="rounded-2xl border border-line bg-white p-5"><div className="mb-4 flex items-center justify-between gap-3"><h3 className="font-bold">{title}</h3>{action}</div>{children}</section>; }
function Metric({ label, value, detail }: { label: string; value: number; detail: string }) { return <div className="border-b border-r border-line p-5"><p className="text-sm text-muted">{label}</p><strong className="mt-2 block text-3xl">{value}</strong><p className="mt-1 text-xs text-muted">{detail}</p></div>; }
function Mini({ label, value }: { label: string; value: React.ReactNode }) { return <div className="rounded-xl bg-mist p-3"><p className="text-xs text-muted">{label}</p><strong className="mt-1 block break-words text-sm">{value}</strong></div>; }
function Info({ label, value }: { label: string; value: string }) { return <div className="flex gap-4 border-b border-line py-3 text-sm last:border-0"><span className="w-24 shrink-0 text-muted">{label}</span><strong className="break-words">{value}</strong></div>; }
function Status({ value }: { value: string }) { const good = ["approved", "active", "verified", "paid", "confirmed", "delivered", "matched"].includes(value); const bad = ["rejected", "suspended", "archived", "exception", "overdue", "failed"].includes(value); return <span className={`rounded-full px-2.5 py-1 font-mono text-xs ${good ? "bg-emerald-50 text-emerald-700" : bad ? "bg-red-50 text-red-700" : "bg-blue-50 text-blue-700"}`}>{humanize(value)}</span>; }
function Score({ label, value }: { label: string; value: number }) { return <div><div className="flex justify-between text-sm"><span>{label}</span><strong>{value.toFixed(0)}%</strong></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-blue-50"><span className="block h-full rounded-full bg-blue-600" style={{ width: `${Math.min(100, value)}%` }} /></div></div>; }
function Empty({ title, body, compact }: { title: string; body: string; compact?: boolean }) { return <div className={`grid place-items-center text-center ${compact ? "py-8" : "min-h-64 p-8"}`}><div><Users className="mx-auto text-blue-300" size={28} /><h2 className="mt-4 font-bold">{title}</h2><p className="mx-auto mt-2 max-w-sm text-sm leading-6 text-muted">{body}</p></div></div>; }
function Hidden({ org, vendor }: { org?: string; vendor: string }) { return <><input type="hidden" name="organizationId" value={org} /><input type="hidden" name="supplierId" value={vendor} /></>; }
function Field({ label, className, ...props }: React.InputHTMLAttributes<HTMLInputElement> & { label: string }) { return <label className={className}><span className="mb-2 block text-sm font-bold">{label}</span><input {...props} className="h-11 w-full rounded-xl border border-line bg-white px-3 text-sm outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-100" /></label>; }
function TextArea({ label, ...props }: React.TextareaHTMLAttributes<HTMLTextAreaElement> & { label: string }) { return <label><span className="mb-2 block text-sm font-bold">{label}</span><textarea {...props} className="min-h-24 w-full rounded-xl border border-line bg-white p-3 text-sm outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-100" /></label>; }
function Select({ name, label, values, defaultValue }: { name: string; label: string; values: string[]; defaultValue?: string }) { return <label><span className="mb-2 block text-sm font-bold">{label}</span><select name={name} defaultValue={defaultValue ?? values[0]} className="h-11 w-full rounded-xl border border-line bg-white px-3 text-sm">{values.map((value) => <option key={value} value={value}>{humanize(value)}</option>)}</select></label>; }
function SelectOptions({ name, label, options, optional }: { name: string; label: string; options: Array<{ value: string; label: string }>; optional?: boolean }) { return <label><span className="mb-2 block text-sm font-bold">{label}</span><select name={name} className="h-11 w-full rounded-xl border border-line bg-white px-3 text-sm">{optional && <option value="">Not mapped</option>}{options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label>; }
function Check({ name, label }: { name: string; label: string }) { return <label className="flex items-center gap-2 rounded-xl border border-line p-3 text-sm"><input type="checkbox" name={name} defaultChecked className="size-4 accent-blue-600" />{label}</label>; }
function humanize(value: string) { return value.replaceAll("_", " ").replace(/^./, (letter) => letter.toUpperCase()); }
